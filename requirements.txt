lhotse==1.33.0
meeteval==0.4.3
nemo_text_processing==1.1.0
nemo_toolkit[asr]==2.7.3
# Pulled in transitively by librosa, but the version matters and it does not constrain it: numba
# 0.66's CUDA target cannot compile two-argument min()/max(), which NeMo's TDT gradient kernel
# uses, so training dies on the first backward pass. Inference is unaffected.
numba==0.65.1
numpy==1.26.4
simplejson==3.19.3
torch==2.9.1
torchaudio==2.9.1
